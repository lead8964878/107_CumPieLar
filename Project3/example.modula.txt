(*
 * Example with Functions
 *)

module Example

// constants
const a = 5;
	  sauce = "xo sauce";
	  out = true;

// variables
var c,f: integer;
	x,j: boolean;

// procedure declaration
procedure add(a:integer, b:integer) : integer
begin
  return a+b;
end add;

procedure minus(alive:boolean, dead:boolean) : boolean
const killer = true;
begin
  return killer&&alive||dead;
end minus;
    
// main function
begin
  c := add(a, 10);
  if (c > 10) then
    print -c;
  else 
    print c;
  end;
  println "Hello World";
end Example.
